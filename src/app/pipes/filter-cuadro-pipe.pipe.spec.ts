import { FilterCuadroPipePipe } from './filter-cuadro-pipe.pipe';

describe('FilterCuadroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCuadroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
